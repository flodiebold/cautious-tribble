apiVersion: v1
kind: Service
metadata:
  name: exampleservice
spec:
  type: NodePort
  selector:
    app: exampleservice
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: otherservice
spec:
  type: NodePort
  selector:
    app: otherservice
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
